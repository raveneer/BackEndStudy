using System.Collections;
using System.Collections.Generic;
using BackEnd;
using UnityEngine;

public class BackEndManager : MonoBehaviour
{
    /*private Initialize(Function callback) -> void

    // example
    private void Start()
    {
        // 서버가 초기화되지 않은 경우
        if (!Backend.IsInitialized)
        {
            // 초기화
            Backend.Initialize(backendCallback);
        }
        else
        {
            backendCallback();
        }
    }*/
}